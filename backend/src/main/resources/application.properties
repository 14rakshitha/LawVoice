spring.application.name=lawvoice-backend
server.port=${PORT:8082}

app.cors.allowed-origin=${FRONTEND_ORIGIN:http://localhost:5170}

# Supabase PostgreSQL Database Configuration (IPv4-compatible pooler for Render)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://aws-0-ap-south-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${JDBC_DATABASE_USERNAME:postgres.bqmwqxyjzipuitrqkgff}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:rakshi14@baskaran}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate properties
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
